{
  "name": "frogger",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "test": "vitest run",
    "build": "tsc && vite build",
    "check": "npm run test && npm run build && npm audit --audit-level=moderate",
    "preview": "vite preview"
  },
  "devDependencies": {
    "pngjs": "^7.0.0",
    "typescript": "^5.6.3",
    "vite": "^8.1.4",
    "vitest": "^3.2.7"
  }
}
